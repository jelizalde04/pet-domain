name: Get Pet By Id Pet Deploy
on:
  push:
    branches: [test]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./get-pet-by-id
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/get-pet-by-id:latest
          build-args: |
            DB_HOST=${{ secrets.DB_HOST }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            DB_PORT=${{ secrets.DB_PORT }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            PET_DB_NAME=${{ secrets.PET_DB_NAME }}
            RESPONSIBLE_DB_NAME=${{ secrets.RESPONSIBLE_DB_NAME }}

  deploy-to-ec2:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key
          ssh-keyscan ${{ secrets.GET_PET_BY_ID_EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Verify SSH connection
        run: ssh -i ~/.ssh/ec2_key ubuntu@${{ secrets.GET_PET_BY_ID_EC2_HOST }} "echo 'SSH connection successful!'"

      - name: Deploy to EC2
        run: |
          ssh -i ~/.ssh/ec2_key ubuntu@${{ secrets.GET_PET_BY_ID_EC2_HOST }} << 'EOF'
          # Stop and remove existing container
          sudo docker stop get-pet-by-id || true
          sudo docker rm get-pet-by-id || true
          
          # Pull the latest image from Docker Hub
          sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/get-pet-by-id:latest
          
          # Run new container with environment variables
          sudo docker run -d \
           --name get-pet-by-id \
           -p 3003:3003 \
           --restart unless-stopped \
           -e DB_HOST=${{ secrets.DB_HOST }} \
           -e DB_USER=${{ secrets.DB_USER }} \
           -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
           -e DB_PORT=${{ secrets.DB_PORT }} \
           -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
           -e PET_DB_NAME=${{ secrets.PET_DB_NAME }} \
           -e RESPONSIBLE_DB_NAME=${{ secrets.RESPONSIBLE_DB_NAME }} \
           "${{ secrets.DOCKERHUB_USERNAME }}/get-pet-by-id:latest"

          # Verify container is running
          sudo docker ps | grep get-pet-by-id
          EOF
